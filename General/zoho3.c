#include <stdio.h>
int main(int argc, char const *argv[]) {
     int i,j,n,x = 0;
     char *string = "WELCOMETOZOHOCORPORATION";
     char array[5][5];
     char input[20];
     n = sizeof(array)/sizeof(1);
     scanf("%s",input);
     for(i = 0;i < 5;i++)
     {
          for(j = 0;j < 5;j++)
          {
               array[i][j] = string[x];
               x++;
          }
     }
     for(i = 0;i < 5;i++)
     {
          for(j = 0;j < 5;j++)
          {
               if(input[k])
          }
     return 0;
}
